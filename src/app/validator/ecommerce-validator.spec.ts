import { EcommerceValidator } from './ecommerce-validator';

describe('EcommerceValidator', () => {
  it('should create an instance', () => {
    expect(new EcommerceValidator()).toBeTruthy();
  });
});
